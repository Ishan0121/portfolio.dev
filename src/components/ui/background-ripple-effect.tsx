// This component is no longer used — replaced by canvas-based ripple in GridBackground.tsx.
// Kept as an empty export to avoid breaking any existing imports.
export const BackgroundRippleEffect = () => null;
